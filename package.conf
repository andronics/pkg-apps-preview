name="preview"
description="File Preview - Quick file content viewer"
version="0.1.0"
method="stow"
collection="apps"
fold=false

# Ignore documentation files
supported_os=("linux")
depends=()
requires_commands=()
ignore=(
    "README.md"
)
